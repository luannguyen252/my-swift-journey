import SwiftUI

struct NotificationView: View {
    var body: some View {
        Text("Breath Animation on watchOS")
    }
}

struct NotificationView_Previews: PreviewProvider {
    static var previews: some View {
        NotificationView()
    }
}
